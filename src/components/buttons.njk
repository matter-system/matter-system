---
title: Buttons
intro: "A button triggers an event or action. They let users know what will happen next."
tag: component
---

{% macro component_box(content) %}
<div
  class="flex flex-col p-4 w-full bg-white border border-gray-200 shadow shadow-md rounded-lg"
  x-data="{ codeOpen: false }"
>
  <div class="flex justify-between">
    <h3 class="text-sm font-medium text-gray-600">Buttons</h3>
    <button
      class="flex items-center justify-center font-medium text-gray-600 w-8 h-8 rounded hover:bg-gray-200 transition duration-200 focus:outline-none cursor-pointer"
      @click=" codeOpen = !codeOpen"
    >
      {% include 'partials/icons/icon_code.njk' %}
    </button>
  </div>
  <div class="flex justify-center items-center p-16">
    {{ content | safe }}
  </div>
  <div class="rounded" x-show="codeOpen">
    {% highlight "html" %}
    {{ content | safe }}
    {% endhighlight %}
  </div>
</div>

{% endmacro %} {% set base_classes = 'px-4 py-3 text-white rounded shadow-sm
focus:outline-none focus:shadow-outline' %} {% set content %}
<div class="space-x-2">
  <button class="{{ base_classes }} bg-indigo-600 hover:bg-indigo-700">
    button one
  </button>
  <button class="{{ base_classes }} bg-pink-500 hover:bg-pink-600">
    button two
  </button>
  <button class="{{ base_classes }} bg-purple-500 hover:bg-purple-600">
    button three
  </button>
</div>
{% endset %}

{{ component_box(content) }}

{{ component_box("Icon Buttons") }}
{{ component_box("Grouped Buttons") }}
