{% set links = [
  {name: 'Overview', path: '/overview'},
  {name: 'Getting started', path: '/getting-started'},
  {name: 'Tokens', path: '/tokens'}
] %}

<p>hkjhkl</p>
<ul class="space-y-2">
  {% for link in links %}
    <li>
      {% if link.path ~ '/' === page.url %}
        <a href="{{ link.path }}" class="pl-3 border-l-4 border-purple-500 text-purple-800 font-semibold">{{ link.name }}</a>
      {% else %}
        <a href="{{ link.path }}" class="text-gray-800" >{{ link.name }}</a>
      {% endif %}
    </li>
  {% endfor %}
</ul>